# 开发日志

## 2026-08-14 - 发布队列内置云发布

- 背景：用户要求养号素材库直接完成账号选择、随机音乐、单条/批量定时及发布，不再先推送到蚁小二页面。
- 决策：素材库复用已部署的 `publish:8010` 蚁小二发布服务，直接在本系统完成草稿创建、图片上传、账号目标、音乐、任务生成和云端提交；本系统保存对应草稿与任务号，便于刷新状态或取消未提交的定时任务。
- 界面：队列改为支持勾选、单条账号/音乐/时间设置、批量账号/间隔定时、批量发布，以及待配置、定时中、已提交、已发布、失败的状态筛选。
- 安全：已创建云端任务的素材禁止再次改写目标，避免重复提交；历史已推送草稿保持原状。连接凭据仍只保留在发布服务的服务器环境变量中。
- 本地验证：`python3 -m py_compile app/main.py app/dingtalk_agent.py`、`node --check static/app.js`、`git diff --check` 均通过；本机没有 Docker，未能本地构建镜像。
- 部署：提交 `7d2d069` 已推送至 `main`。由于标准部署脚本尚未登记 `nurture-library` 服务，已通过腾讯云服务器终端创建可回滚的完整备份、保留 `.env` / `data` / `media` 并重建该单一容器。随后提交 `e307343` 为静态 CSS/JS 增加版本参数，避免旧浏览器缓存破坏发布工具栏布局。
- 线上验证：已在 `https://nurture.xinjieai.com/?release=e307343` 实际渲染确认批量账号、随机音乐、批量定时、批量发布和单条保存设置均可见；页面加载了 18 个已授权发布账号，旧“推送到蚁小二”按钮不再出现。未创建、提交或发布任何真实素材。

## 2026-08-12

- 增加钉钉 Stream 常驻养号员工：群图片接收、单消息多图归组、标题/正文/话题预览、5 分钟重生成窗口、自动入库。
- 增加账号排序与最低待发库存优先分配；按账号间隔生成下次排期。
- 增加到点推送蚁小二草稿、群内每日待发素材报告与运行状态接口。

## 2026-08-13 - 到期禁用素材预览操作

- 钉钉预览卡片在倒计时到期并自动入库后，会原位更新为“调整已结束 · 已自动入库”。“放弃入库”“换一版”均改为禁用状态，不能再触发操作。
- H5 实时预览页同步显示 `00:00`、自动入库提示和不可点击的灰色按钮；已自动入库的预览页仍可打开以展示最终状态。
- 本地已执行 Python 编译与卡片状态断言校验；代码已推送至 `d39e8e4`。
- 线上部署待恢复：GitHub Actions 的 `DEPLOY_HOST` Secret 为空，工作流在连接服务器前失败，尚未完成线上视觉验收。

## 2026-08-13 - 换一版原卡更新与兜底文案自动重试

- 保留所有待确认素材每 10 秒更新倒计时的逻辑。
- “换一版”及其卡片回调路径均限制为仅更新当前任务既有的 `card_biz_id`；如果原卡更新失败，不允许退化为新发卡片或文本消息，等待该卡片后续的 10 秒刷新重试。
- 首次生成结果出现“今天的小日常”或“今天留下一点日常。”时，立即带更严格的提示自动重试一次；第二次仍失败才保留安全兜底。
- 本地 Python 编译及自动重试、禁止新发卡片断言通过；代码提交 `631e0cd`。线上自动部署仍受 `DEPLOY_HOST` 缺失阻塞。

## 2026-08-13 - 钉钉素材流程稳定性修复

- 将低质量文案判定集中处理：标题过短、正文不足 30 字、少于 2 个话题，或命中“今天的小日常”等模板兜底文本时，都会自动重试一次；连续失败会明确报错，不再把充数文案写入素材库。
- 放弃入库后，原互动卡片会原位更新为“该素材已放弃入库”，两个操作按钮同步禁用，避免误显示为自动入库。
- 调度器在执行已提交操作前再次核对截止时间，避免倒计时结束后仍换一版或放弃入库；同一钉钉消息重复投递也会被忽略。
